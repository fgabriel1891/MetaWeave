% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maxent.R
\name{maxent_model}
\alias{maxent_model}
\title{Maximum-entropy ecological network model}
\usage{
maxent_model(
  row_group,
  column_group = row_group,
  constraint = c("connectance", "links", "degree_sequence"),
  connectance = NULL,
  links = NULL,
  out_degree = NULL,
  in_degree = NULL,
  ensemble_size = 100L,
  self_links = FALSE,
  seed = 1L,
  burn_in = 1000L,
  thin = 100L,
  keep_ensemble = FALSE
)
}
\arguments{
\item{row_group}{Assemblage group represented by matrix rows (consumers).}

\item{column_group}{Assemblage group represented by matrix columns
(resources). May equal \code{row_group} for a directed unipartite food web.}

\item{constraint}{One of \code{"connectance"}, \code{"links"}, or
\code{"degree_sequence"}.}

\item{connectance}{Desired connectance in \verb{[0, 1]}. Converted to an exact
local link count with \code{round(connectance * allowed_dyads)}.}

\item{links}{Exact number of interactions in every local ensemble member.}

\item{out_degree}{Row sums (numbers of resources per consumer).}

\item{in_degree}{Column sums (numbers of consumers per resource).
Degree constraints may be named numeric vectors or functions accepting the
locally present species names and returning a numeric vector.}

\item{ensemble_size}{Number of adjacency matrices used for marginal
probabilities.}

\item{self_links}{Whether a species may interact with itself when its name
appears in both groups.}

\item{seed}{Integer seed. A cell identifier is added to this seed so spatial
cells receive distinct reproducible ensembles. The previous global random
state is restored after inference. Use \code{NULL} to use normal R RNG behavior.}

\item{burn_in}{Number of degree-preserving swap proposals before recording a
degree-constrained ensemble.}

\item{thin}{Number of swap proposals between recorded matrices.}

\item{keep_ensemble}{Store binary ensemble members in network metadata.}
}
\value{
An \code{inference_model} compatible with \code{\link[=infer_network]{infer_network()}} and
\code{\link[=infer_networks]{infer_networks()}}.
}
\description{
Constructs a directed network inference backend that generates binary
adjacency matrices under hard structural constraints and returns their
marginal species-pair probabilities. Rows are consumers, columns are
resources, and \code{matrix[i, j] = 1} means consumer \code{i} uses resource \code{j}.
}
\details{
For \code{constraint = "connectance"} or \code{"links"}, networks are sampled
uniformly from all allowed adjacency matrices with exactly the requested
number of links. This is the maximum-entropy distribution under a hard link
count. For \code{constraint = "degree_sequence"}, a feasible matrix is found by
maximum flow and randomized with degree-preserving checkerboard swaps. This
is an approximate uniform MCMC ensemble conditional on the supplied margins.

The implementation follows the constrained, minimally biased ensemble
rationale of Banville, Gravel & Poisot (2023), but does not yet implement
their SVD-entropy objective or simulated-annealing optimizer.
}
\examples{
assemblage <- new_assemblage(list(food_web = c("a", "b", "c")))
model <- maxent_model(
  row_group = "food_web",
  constraint = "connectance",
  connectance = 0.25,
  ensemble_size = 50,
  self_links = FALSE,
  seed = 42
)
network <- infer_network(assemblage, model)
network$matrix
}
\references{
Banville F, Gravel D, Poisot T (2023). What constrains food
webs? A maximum entropy framework for predicting their structure with
minimal biases. PLOS Computational Biology 19: e1011458.
\doi{10.1371/journal.pcbi.1011458}
}
